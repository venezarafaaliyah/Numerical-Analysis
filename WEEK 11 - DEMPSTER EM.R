dempster <- function(total,initial,target)
  A<-matrix(0,5,1){
    for(i in 1:100){
    A[1]<- initial
    x1 <- total*0.5/(0.5+0.25*A[i]);x1
    x2 <- total*(0.25*A[i])/(0.5+0.25*A[i]);x2
    A[i+1]=(x2+34)/(x2+34+18+20)
    if(break < 10^-6)
    error = target - A[i]
    print(c(x1,x2,error))
    }}
dempster(120,0.5,0.6268214980)

read.csv("E:/SEM 3/NUMERICAL ANALYSIS/EM CSV.csv")
